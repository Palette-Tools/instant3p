// CLI exports for schema and perms files
// This module is used when the CLI transforms imports in schema/perms files
// from "@instant3p/electron" to "@instant3p/electron/dist/cli"

export * from '@instantdb/core';